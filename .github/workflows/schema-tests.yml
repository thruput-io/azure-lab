name: 'Schema Tests (Avro + Azure EH Schema Registry)'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  schema-tests:
    name: 'Schema Tests'
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    # ---------------------------------------------------------------
    # Write client.properties from the repository secret.
    # The secret KAFKA_CLIENT_PROPERTIES holds the full file content
    # (populated credentials — do NOT commit to git).
    # All values (bootstrap, topic, OAuth) come from this file.
    # No Azure login or Key Vault call is needed.
    # ---------------------------------------------------------------
    - name: Load client.properties
      id: creds
      run: |
        set -euo pipefail
        PROPS_FILE="/tmp/client.properties"
        printf '%s' "${{ secrets.KAFKA_CLIENT_PROPERTIES }}" > "$PROPS_FILE"
        sed -i 's/\r$//' "$PROPS_FILE"

        if [ ! -s "$PROPS_FILE" ]; then
          echo "ERROR: KAFKA_CLIENT_PROPERTIES secret is empty — set it in repo Settings → Secrets"
          exit 1
        fi
        if ! grep -q '^bootstrap.servers=' "$PROPS_FILE"; then
          echo "ERROR: client.properties is missing bootstrap.servers"
          exit 1
        fi
        if ! grep -q '^topic=' "$PROPS_FILE"; then
          echo "ERROR: client.properties is missing topic"
          exit 1
        fi

        BOOTSTRAP=$(grep '^bootstrap.servers=' "$PROPS_FILE" | cut -d= -f2-)
        DOMAIN=$(echo "$BOOTSTRAP" | cut -d: -f1)

        echo "domain=$DOMAIN"         >> $GITHUB_OUTPUT
        echo "props_file=$PROPS_FILE" >> $GITHUB_OUTPUT
        echo "Loaded: bootstrap=$BOOTSTRAP"

    # ---------------------------------------------------------------
    # Check 1 — Connectivity + SSL
    # TLS handshake on :443 (HTTPS) and :9093 (Kafka)
    # ---------------------------------------------------------------
    - name: 'Check 1 — Connectivity + SSL'
      id: check1
      run: |
        set -euo pipefail
        DOMAIN="${{ steps.creds.outputs.domain }}"

        echo "==> [1a] TLS certificate on ${DOMAIN}:443 ..."
        CERT_RESULT=$(echo | openssl s_client -connect "${DOMAIN}:443" -servername "$DOMAIN" 2>/dev/null)
        if ! echo "$CERT_RESULT" | grep -q "Verify return code: 0 (ok)"; then
          echo "FAIL: TLS certificate verification failed on :443"
          exit 1
        fi
        EXPIRY=$(echo "$CERT_RESULT" | openssl x509 -noout -enddate 2>/dev/null | cut -d= -f2)
        EXPIRY_EPOCH=$(date -d "$EXPIRY" +%s)
        DAYS_LEFT=$(( (EXPIRY_EPOCH - $(date +%s)) / 86400 ))
        echo "  Certificate expires in $DAYS_LEFT days ($EXPIRY)"
        if [ "$DAYS_LEFT" -lt 7 ]; then
          echo "FAIL: Certificate expires in $DAYS_LEFT days — renew immediately"
          exit 1
        fi
        echo "PASS: :443 TLS certificate valid ($DAYS_LEFT days remaining)"

        echo "==> [1b] TLS handshake on ${DOMAIN}:9093 (Kafka) ..."
        if ! echo | openssl s_client -connect "${DOMAIN}:9093" -servername "$DOMAIN" 2>/dev/null \
            | grep -q "Verify return code: 0 (ok)"; then
          echo "FAIL: TLS certificate verification failed on :9093"
          exit 1
        fi
        echo "PASS: :9093 TLS handshake successful"

    # ---------------------------------------------------------------
    # Check 2 — Authentication
    # 2a: Entra ID OAuth token (curl)
    # 2b: Kafka broker auth via confluentinc/cp-kafka kafka-topics
    # 2c: Azure Schema Registry REST API auth (curl + bearer token)
    # ---------------------------------------------------------------
    - name: 'Check 2 — Authentication (OAuth + Kafka + Schema Registry)'
      id: check2
      run: |
        set -euo pipefail
        PROPS_FILE="${{ steps.creds.outputs.props_file }}"

        TOKEN_ENDPOINT=$(grep '^sasl.oauthbearer.token.endpoint.url=' "$PROPS_FILE" | cut -d= -f2-)
        JAAS=$(grep '^sasl.jaas.config=' "$PROPS_FILE" | cut -d= -f2-)
        CLIENT_ID=$(echo "$JAAS"     | grep -oP 'clientId="\K[^"]+')
        CLIENT_SECRET=$(echo "$JAAS" | grep -oP 'clientSecret="\K[^"]+')
        BOOTSTRAP=$(grep '^bootstrap.servers=' "$PROPS_FILE" | cut -d= -f2-)
        DOMAIN=$(echo "$BOOTSTRAP" | cut -d: -f1)
        SR_URL="https://${DOMAIN}"

        echo "==> [2a] Fetching Entra ID OAuth token ..."
        TOKEN_RESP=$(curl -sS --max-time 15 -X POST "$TOKEN_ENDPOINT" \
          -d "grant_type=client_credentials" \
          -d "client_id=$CLIENT_ID" \
          --data-urlencode "client_secret=$CLIENT_SECRET" \
          -d "scope=https://eventhubs.azure.net/.default")
        ACCESS_TOKEN=$(echo "$TOKEN_RESP" | grep -oP '"access_token":\s*"\K[^"]+' || true)
        if [ -z "$ACCESS_TOKEN" ]; then
          echo "FAIL: Could not obtain OAuth token — response: $TOKEN_RESP"
          exit 1
        fi
        echo "PASS: OAuth token acquired"

        echo "==> [2b] Kafka broker auth via confluentinc/cp-kafka kafka-topics ..."
        docker run --rm \
          -v "$PROPS_FILE:/tmp/client.properties:ro" \
          confluentinc/cp-kafka:7.9.0 \
          kafka-topics \
            --bootstrap-server "$BOOTSTRAP" \
            --command-config /tmp/client.properties \
            --list \
          && echo "PASS: Kafka broker auth OK"

        echo "==> [2c] Azure Schema Registry REST API auth ..."
        SR_RESP=$(curl -sS --max-time 15 \
          -H "Authorization: Bearer ${ACCESS_TOKEN}" \
          "${SR_URL}/\$schemagroups")
        if echo "$SR_RESP" | grep -q '"value"'; then
          echo "PASS: Schema Registry auth OK — groups listed"
        else
          echo "FAIL: Schema Registry auth failed — response: $SR_RESP"
          exit 1
        fi

    # ---------------------------------------------------------------
    # Check 3 — Avro Produce + Consume via scripts/schema-check.sh
    # Uses official confluentinc/cp-schema-registry image.
    # Only input: client.properties
    # Run locally: ./scripts/schema-check.sh --props-file client.properties
    # ---------------------------------------------------------------
    - name: 'Check 3 — Avro Produce + Consume (confluentinc/cp-schema-registry)'
      id: check3
      run: |
        set -euo pipefail
        chmod +x scripts/schema-check.sh
        scripts/schema-check.sh --props-file "${{ steps.creds.outputs.props_file }}"

    # ---------------------------------------------------------------
    # Summary
    # ---------------------------------------------------------------
    - name: Summary
      if: always()
      run: |
        printf '%s\n' "### Schema Tests Results" >> $GITHUB_STEP_SUMMARY
        printf '%s\n' "" >> $GITHUB_STEP_SUMMARY
        printf '%s\n' "| # | Check | Target |" >> $GITHUB_STEP_SUMMARY
        printf '%s\n' "|---|---|---|" >> $GITHUB_STEP_SUMMARY
        printf '%s\n' "| 1 | Connectivity + SSL | \`:443\` TLS cert + \`:9093\` TLS handshake |" >> $GITHUB_STEP_SUMMARY
        printf '%s\n' "| 2 | Authentication | OAuth token + Kafka \`kafka-topics\` + Schema Registry REST |" >> $GITHUB_STEP_SUMMARY
        printf '%s\n' "| 3 | Avro Produce + Consume | \`scripts/schema-check.sh\` via \`confluentinc/cp-schema-registry\` |" >> $GITHUB_STEP_SUMMARY
